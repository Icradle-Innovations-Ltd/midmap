# Mind Bloom

A calm mind-map style web app built with React and Express. It uses no database and keeps the game content in local server files.

## Scripts

- `npm install`
- `npm run dev`
- `npm run build`
- `npm start`

## Stack

- React + Vite
- Express
- Local in-memory game content only
